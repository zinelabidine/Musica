(function () {
  "use strict";

  angular.module('app').factory('profilService', ['$http', '$cookies', 'EnvironmentConfig', '$filter', function ($http, $cookies, EnvironmentConfig, $filter) {
    return {
    }
  }]);
}());
